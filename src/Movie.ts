export type Movie = {
  posterLink: string;
  rank: string;
  trend: string;
  title: string;
  year: string;
  genre: string;
  votes: string;
  rating: string;
  emojiSummary: string;
  index: string;
};

export type EmojiFrequency = {
  emoji: string;
  frequency: number;
};
